<script type="text/json" id="bootstrap">
  { "businesses": <%= render "index.rabl" %>,
    <% session = (logged_in? ? "{\"username\":\"#{ current_user.username }\" }" : "null") %>
    "session": <%= session %>
    }
</script>